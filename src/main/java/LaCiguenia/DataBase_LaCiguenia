CREATE DATABASE la_ciguenia;

USE la_ciguenia;

CREATE TABLE login_usuario (
	login_id INT PRIMARY KEY AUTO_INCREMENT,
    logi_email VARCHAR(50),
    logi_password VARCHAR(50)
);

SELECT * FROM login_usuario;
DELETE FROM login_usuario WHERE login_id = 1;